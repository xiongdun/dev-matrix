# pre-commit

## 触发时机

Agent 生成代码补丁后、提交前

## 执行内容

1. 运行 `ruff check` 检查代码风格
2. 运行 `pytest` 确保测试通过
3. 检查是否有硬编码的密钥或 Token
4. 检查是否有 `print()` 语句残留

## 失败处理

- 任何检查失败则阻止提交
- 将错误信息反馈给 Agent 自动修复
- 修复后重新执行检查
